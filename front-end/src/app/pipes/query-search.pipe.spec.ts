import { QuerySearchPipe } from './query-search.pipe';

describe('QuerySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuerySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
